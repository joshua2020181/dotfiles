# Machine-specific environment variables.
# Copy to ~/.env.local and fill in values — this file is never committed.
#
#   cp ~/.env.local.template ~/.env.local
#

# Short name shown in the terminal prompt (e.g. "laptop", "ec2", "workstation")
MACHINE_NAME="my-machine"
